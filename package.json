{
  "name": "simple",
  "version": "1.0.0",
  "description": "A simple test",
  "main": "simple.js",
  "scripts": {
    "test": "jest --coverage",
    "coverage": "jest --coverage && coveralls < coverage/lcov.info"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.1.1",
    "jest": "^29.7.0"
  }
}
